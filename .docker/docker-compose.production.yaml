services:
  webapp:
    image: ghcr.io/gscd-dev/a11y-market-web:latest
    container_name: a11y-market-web
    restart: unless-stopped
    networks:
      - proxy
    labels:
      # Traefik setting
      - 'traefik.enable=true'
      # Router setting
      - 'traefik.http.routers.a11y-market-web.rule=Host(`${HOST_NAME}`)'
      - 'traefik.http.routers.a11y-market-web.entrypoints=websecure'
      - 'traefik.http.routers.a11y-market-web.tls=true'
      # Service Setting
      - 'traefik.http.services.a11y-market-web.loadbalancer.server.port=80'

networks:
  proxy:
    external: true
